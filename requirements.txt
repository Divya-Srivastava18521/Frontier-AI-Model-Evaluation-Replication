# Core harness has zero required dependencies (stdlib only).
# Optional backends:
transformers>=4.40
torch --index-url https://download.pytorch.org/whl/cpu
accelerate>=0.30
openai>=1.30
inspect-ai>=0.3
matplotlib>=3.8
